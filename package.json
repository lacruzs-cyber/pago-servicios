{
  "name": "pago-servicios",
  "version": "1.0.0",
  "description": "Administrador de vencimientos de servicios mensuales",
  "scripts": {
    "backend": "node backend/server.js",
    "frontend": "npm run dev --prefix app",
    "dev": "concurrently -n BACKEND,FRONTEND -c cyan,green \"npm run backend\" \"npm run frontend\"",
    "install:backend": "npm install --prefix backend",
    "install:frontend": "npm install --prefix app",
    "install:all": "npm run install:backend && npm run install:frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
